services:
  - type: web
    name: hr-ai-portal
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: HR_PORTAL_USER
        value: coordinator
      - key: HR_PORTAL_PASSWORD
        sync: false
